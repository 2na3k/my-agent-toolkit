# Convert to something

Do the work of reading the files, summarize it and convert it to the specific format.

Or, if not a files then it convert a format to another format.

Example:
```text
Q: Convert this 1,2,3 to a list of string
A: ["1", "2", "3"]
```

And it's copiable.